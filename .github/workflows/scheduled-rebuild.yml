---
name: Scheduled clean rebuild

on:
  schedule:
    - cron: "17 23 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/ci.yml
    with:
      pull: true
      images: all
